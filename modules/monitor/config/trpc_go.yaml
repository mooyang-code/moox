global:
  namespace: Development
  env_name: dev

server:
  timeout: 5000
  service:
    - name: trpc.moox.monitor.MonitorMgr
      port: 11410
      network: tcp
      protocol: http
      timeout: 30000

plugins:
  log:
    default:
      - writer: console
        level: info
