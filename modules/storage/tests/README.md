# storage 测试目录约定

`tests/` 用于放置跨包契约、schema、端到端验收等测试。普通单元测试仍然放在被测包旁边。

当前约定：

- `tests/schema/`：验证 `modules/storage/schema/` 下的表定义与当前概念模型一致。
- `internal/**`：只保留贴近具体包实现的单元测试或包内集成测试。

如果测试需要跨越多个 service 或验证仓库级约束，优先放到 `tests/`；如果测试需要访问包内未导出函数，应留在对应包内，并在测试名中说明行为目标。
