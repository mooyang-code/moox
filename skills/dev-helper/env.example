# 本地开发者配置示例（勿提交真实值）
#
# 用法一：运行脚本时按提示输入 user@host（可选保存到本文件）
#   cp skills/dev-helper/env.example ~/.moox-dev.env
#
# 用法二：legacy 兼容，可写入 infra/infra.local.yaml 的 remote.ssh（该文件已 gitignore）
# 注意：infra.local.yaml 不再保存服务部署拓扑，服务 IP/port 请通过 t_service_deployments/SysDeploy 管理。
#
# 切勿将 SSH 密码、云密钥写入本仓库任何文件。请使用 SSH 公钥登录。

# SSH 目标，例如 ubuntu@your-dev-host
# MOOX_DEV_SSH_TARGET=ubuntu@<deploy-host>

# 可选：覆盖远端路径
# MOOX_DEV_DEPLOY_DIR=/home/ubuntu/moox
# MOOX_DEV_REMOTE_GO=/home/ubuntu/.local/go1.24.0/bin/go
